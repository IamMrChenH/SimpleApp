<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chenhao.simpleapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.HomeActivity"
            android:label="@string/title_activity_home">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".ui.activity.EnvirActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".ui.fragment.EnvirFragment"
            android:screenOrientation="landscape" />
        <activity android:name=".ui.fragment.TrafficFragment" />
        <activity android:name=".ui.activity.StreetLedActivity" />
        <activity android:name=".ui.activity.RedLedManageActivity" />
        <activity
            android:name=".user.ui.ui.activity.UserHomeActivity"
            android:label="@string/title_activity_user_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".user.ui.ui.activity.RecordActivity" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme3" />
        <activity
            android:name=".ui.activity.WelcomeActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.RegisterActivity" />
        <activity
            android:name=".ui.activity.PermissionActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".ui.activity.YuZhiActivity" />
        <activity android:name=".ui.activity.CarAddActivity" />
        <activity android:name=".ui.activity.CarRecordActivity" />
        <activity android:name=".user.ui.ui.activity.CarActivity" />
        <activity android:name=".user.ui.ui.activity.CarInfoActivity" />
        <activity android:name=".ui.activity.VersionActivity"></activity>
    </application>

</manifest>